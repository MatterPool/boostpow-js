'use strict';
var expect = require('chai').expect;
var index = require('../dist/index.js');

describe('boostrank', () => {
   it('should sort empty list and null edge cases', async () => {
      expect(true).to.eql(false)
   });

   it('should sort empty list and 1, 2, and 5 items', async () => {
      expect(true).to.eql(false)
   });
});
